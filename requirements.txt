# Core ML
torch>=2.0.0
torchvision>=0.15.0
timm>=0.9.0                      # EfficientNet / ConvNeXt / Swin for GenConViT

# HuggingFace ecosystem — used by all primary models
transformers>=4.35.0             # CommunityForensics ViT, wav2vec2, HuBERT
huggingface_hub>=0.19.0          # weight downloads (GenConViT, MesoNet conversion)
accelerate>=0.24.0               # mixed-precision inference

# Face detection
mediapipe>=0.10.0                # BlazeFace: sub-ms face detection (Bazarevsky et al., 2019)
opencv-python>=4.8.0

# Audio processing
sounddevice>=0.4.6               # cross-platform audio I/O
numpy>=1.24.0
librosa>=0.10.0                  # log-mel spectrograms (LCNN backend)
webrtcvad>=2.0.10                # Google WebRTC VAD for mic silence detection

# Screen capture
mss>=9.0.1                       # cross-platform screen capture

# OS notifications
plyer>=2.1.0                     # cross-platform OS notifications

# Utilities
tqdm>=4.65.0
requests>=2.31.0
Pillow>=10.0.0
scipy>=1.11.0

# For MesoNet Keras→PyTorch conversion only (not required at runtime)
# h5py>=3.9.0
